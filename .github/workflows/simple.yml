name: Shell Command

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eccho "Hello World"
      - name: "Multi Line Stript"
        run: |
          node -v
          npm -v